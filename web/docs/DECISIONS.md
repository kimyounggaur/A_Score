# ScoreStore 결정 기록

추천 기본값에서 변경하면 이유·날짜·영향 범위를 이 문서에 추가한다.

| ID  | 결정        | 채택값                                                                           | 이유                                                                                |
| --- | ----------- | -------------------------------------------------------------------------------- | ----------------------------------------------------------------------------------- |
| D1  | 기능 기준본 | 화면 흐름=`ScoreStore.dc.html`, 토큰·문구=`index.html`, 관리자 골격=`admin.html` | 각 레거시의 가장 완성된 부분만 참고한다.                                            |
| D2  | 사업 모델   | 단일 판매자 + 편곡자 크레딧                                                      | 정산·세무·판매자 약관 부담을 만들지 않는다. seller 역할과 출금 설정은 제거한다.     |
| D3  | 포인트      | 유상 lot과 무상 lot 분리; Stage B 모두 모의                                      | 환불·만료 규정을 구분할 수 있다. 실서비스는 적립부터 열고 충전은 약관 검토 뒤 연다. |
| D4  | 비회원 구매 | 회원만 구매                                                                      | 보관함·재다운로드·워터마크 식별에 계정이 필요하다.                                  |
| D5  | 저장소·배포 | 같은 저장소 `web/` + Vercel Root Directory=`web`                                 | 기존 Pages 데모를 컷오버까지 유지한다.                                              |
| D6  | 상품 이미지 | Stage B는 기보 형식별 결정적 SVG, 실서비스는 워터마크 1페이지                    | 썸네일이 TAB·오선·드럼보 등 구매 정보를 전달해야 한다.                              |
| D7  | 상품 URL    | 숫자 ID                                                                          | `/scores/1006` 형태로 인코딩·로마자 변환 비용을 피한다.                             |
| D8  | 다국어      | 보류, 한국어 전용                                                                | 날짜·통화 포맷만 `lib/format.ts`에 모은다.                                          |
| D9  | 브랜드 폰트 | MaruBuri는 고정 문구만, 본문은 앱 글리프 Noto Sans KR 서브셋                     | 한글 unicode-range 다중 요청을 없애되 미포함 동적 글자는 시스템 폰트로 대체한다.    |

## 후속 결정

| 날짜       | ID  | 결정/변경                                                     | 이유와 영향                                                                                                                                            |
| ---------- | --- | ------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------ |
| 2026-09-13 | D3  | 로그아웃 시 장바구니와 찜은 유지                              | 다시 로그인한 사용자가 탐색 상태를 잃지 않게 한다.                                                                                                     |
| 2026-09-13 | D9  | Noto Sans KR을 단일 107KB 앱 서브셋으로 전환                  | 모바일 Lighthouse에서 다중 한글 폰트 요청으로 생긴 반복 레이아웃을 제거한다. OFL은 자산과 함께 배포한다.                                               |
| 2026-09-13 | D10 | Stage B 사이트명은 브라우저 표시 설정                         | localStorage 기반이라 헤더·푸터·관리자 표기와 현재 문서 메타만 갱신한다. 크롤러용 SSR/OG 설정은 Stage C 서버 저장소에서 전환한다.                      |
| 2026-09-13 | D11 | Stage B 배포는 DEMO 표시 유지                                 | 실 결제·다운로드·법적 검토가 연결되기 전에는 실서비스로 오인되지 않게 한다.                                                                            |
| 2026-09-13 | D12 | 공개 번들은 기능별 repository facade와 빌드 시 seed 검증 사용 | 화면의 mock 직접 접근 금지는 유지하면서, aggregate barrel과 Zod를 공개 초기 JS에서 제외해 180KB 예산을 지킨다. 관리자 쓰기 경계는 계속 Zod로 검증한다. |
| 2026-09-13 | D13 | Stage B 데모 모드는 명시적 `false`가 아니면 활성화            | 환경 변수 누락 시 mock 인증·결제가 실서비스처럼 보이지 않게 한다. `false` 전환은 Stage C 외부·기술 게이트를 모두 통과한 뒤에만 허용한다.               |
| 2026-09-13 | D14 | 최초 판매 가능 seed만 canonical 정적 상세로 생성              | 판매 전·숨김 seed는 HTTP 404를 유지한다. 브라우저 관리자에서 새로 게시한 상품은 noindex `/local` 경로를 사용하고, 상세 가격은 60초 ISR로 갱신한다.     |
